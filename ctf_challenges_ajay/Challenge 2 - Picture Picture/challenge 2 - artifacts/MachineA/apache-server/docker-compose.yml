version: '3.8'

services:
  apache:
    image: httpd:latest
    container_name: apache-directory-server
    ports:
      - "8080:80"
    volumes:
      - ./public_html:/usr/local/apache2/htdocs/
      - ./httpd.conf:/usr/local/apache2/conf/httpd.conf
